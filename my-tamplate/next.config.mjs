/** @type {import('next').NextConfig} */
/*const nextConfig = {};
module.exports = {
    reactStrictMode: true,
  };
export default nextConfig;*/
const nextConfig = {
    reactStrictMode: true,
  };
  
  export default nextConfig;
   
